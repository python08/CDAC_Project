import { SideBarData } from "../SideBarData";
import "./login.css"
import "../teacher.css"

const SideBar = (teacherid,instituteid)=>{


{/* <div>

            
<div className="height">
    <div><img src={image} className="image"  /> </div> 
    <div>Institute Name</div>
</div>
<ul className="SidebarList" >
  {SideBarData.map((val,key)=>{
      
      return (
          <li key={key} className="row"  onClick={()=>window.location.pathname = val.link+"/"+teacherid}>
            <div className="effect" id="title"  >{val.title}</div>
          </li>
      );
  })}
</ul>

</div> */}


console.log(teacherid)
    return(
        <div>

            
                      
                
        </div>
    )

}
export default SideBar